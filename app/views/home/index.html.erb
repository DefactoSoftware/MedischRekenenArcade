<div class="row">
  <div class="three-column-small">
    <%= render 'user_left_sidebar' unless current_user.guest? %>
  </div>
  <div class="three-column-big block center">
    <h1><%= personalized_welcome(current_user)%></h1>
    <div class="row">
      <%= render "#{current_user.class.name.downcase}_center" %>
    </div>
    <div class="row center">
      <div id="call_to_action_buttons one-column-fill">
        <%= call_to_action_buttons(current_user) %>
      </div>
    </div>
  </div>
  <div class="three-column-small">
    <%= render 'user_right_sidebar' unless current_user.guest? %>
  </div>
</div>
